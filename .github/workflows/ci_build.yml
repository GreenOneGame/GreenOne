name: Build

on:
  push:
    branches:
      - 'feature-*'
  pull_request:
    branches: ['dev', 'release', 'master']

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name : Debug Git
        run: git log

      - name: Run build CI
        shell: powershell
        run: |
            cd "C:\Users\SERV-ADMIN\Desktop\GreenOneProject\"
            .\build_ue5.ps1