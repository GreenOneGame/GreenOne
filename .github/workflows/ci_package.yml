name: Package

on:
  pull_request:
    branches:
      - 'master'

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Debug Git
        run: git log

      - name: Run Package 
        shell: powershell
        run: |
            cd "C:\Users\SERV-ADMIN\Desktop\GreenOneProject\"
            .\package_ue5.ps1